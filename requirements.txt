# Pinned to the versions this project was built and tested against.
# Loose ranges let a future release change behaviour silently, which for a
# bit-exact scheme like this means "the message stops decoding".
numpy==2.4.2
scipy==1.17.1
scikit-learn==1.8.0
pillow==12.1.1
joblib==1.5.3
matplotlib==3.11.1
pytest==9.1.1
